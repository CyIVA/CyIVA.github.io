# cyiva 개발 작업 규칙

## 작업 시작

1. 이 문서, `docs/DEVELOPMENT_PLAN.md`, `docs/FILE_MAP.md`, `docs/DEVELOPMENT_STATUS.md`를 읽는다.
2. `git status`와 관련 파일을 확인하여 기존 변경 사항을 보존한다. 문서의 구현 상태를 실제 코드와 대조한다.
3. 현재 요청 범위와 다음 작업을 확인한다. 사용자 지시가 이 가이드보다 우선한다. 구체적인 구현 선택은 확정된 요구사항 안에서 진행한다.

## 문서 관리

- 개발 계획의 기준 원본은 `docs/DEVELOPMENT_PLAN.md`다. 설계 폴더의 이전 문서는 원본 위치 안내만 유지한다.
- 디렉토리·파일 추가, 수정, 이동, 삭제 시 `docs/FILE_MAP.md`를 확인하고 경로·한 줄 역할을 실제 상태에 맞춘다. 역할이 그대로인 수정은 설명을 유지해도 된다.
- 직접 관리하는 코드·설정·문서는 파일별로 기록한다. 동일한 형식의 포스트·연혁·프로젝트는 컬렉션별 패턴으로 묶는다. 새 자산은 용도별 그룹으로 기록하고 특별한 역할은 개별 명시한다.
- `node_modules`, `dist`, 캐시와 Git 내부 파일은 파일 지도에서 제외한다. 아직 만들지 않은 파일은 현재 지도에 넣지 않는다.
- `docs/DEVELOPMENT_STATUS.md`에 기능 상태, 검증 근거, 남은 작업, 다음 작업과 중요한 설계 결정을 기록한다. 기능 ID는 계획서와 일치시킨다.

## 구현 원칙

- Astro 정적 사이트와 일반 Markdown/YAML을 사용한다. Liquid 변환은 초기 이관에서 한 번만 수행한다.
- 글·프로젝트·소개 정보를 화면 컴포넌트와 분리한다. 새 콘텐츠와 카테고리 게시에 코드·라우트 수동 등록을 요구하지 않는다.
- 공개 여부는 공통 빌드 필터로 처리한다. 숨긴 콘텐츠가 HTML·클라이언트 데이터·목록·집계에 포함되지 않게 한다.
- 외부 라이브러리 추가 전 기본 기능으로 해결 가능한지 확인한다. 추가 이유·대안·운영 영향을 개발 현황에 기록하고 lockfile 및 런타임 버전 설정을 관리한다.
- 기존 콘텐츠 변환 전 복원 지점을 확인한다. 제목·날짜·본문 의미를 보존하고 테스트 글과 샘플을 임의로 삭제하지 않는다.
- 사용자 소개·경력·프로젝트 성과는 확인된 사실만 사용한다. 임시 문구와 미확인 자료는 구분한다.

## 검증과 작업 종료

- 상태는 예정 / 진행 중 / 구현 완료·검증 전 / 검증 완료 / 보류로 구분한다. 구현 유무와 검증 여부를 분리한다.
- 변경 범위에 맞는 검증을 수행한다. 콘텐츠 변환은 렌더링·링크, 동작 변경은 해당 기능·경계 조건, UI 변경은 모바일·키보드·양쪽 테마를 확인한다.
- 코드 변경 시 구성된 빌드 및 관련 검사를 실행한다. 아직 없는 명령을 실행 가능하다고 기록하지 않는다. 문서만 수정한 경우 문서 정합성·경로·diff 확인으로 검증한다.
- 실패하거나 실행하지 못한 검증은 이유와 함께 현황 문서에 기록한다. 검증 없이 완료 처리하지 않는다.
- 종료 전에 파일 지도·개발 현황·필요 시 계획서를 갱신하고 변경 diff를 확인한다. 결과와 남은 문제를 사용자에게 알린다.
- 로컬 저장, Git 커밋, 원격 push, 공개 배포는 서로 구분하여 보고한다. 현재 요청 범위 밖의 게시를 완료했다고 표현하지 않는다.
